---
import MainLayout from '../../layouts/MainLayout.astro';
import fs from 'fs';
import path from 'path';

export async function getStaticPaths() {
    const teamsDir = path.join(process.cwd(), 'src', 'content', 'teams');
    
    // Check if directory exists
    if (!fs.existsSync(teamsDir)) {
        return [];
    }
    
    const teamFiles = fs.readdirSync(teamsDir).filter(file => file.endsWith('.md'));
    
    return teamFiles.map(file => {
        const slug = file.replace('.md', '');
        const content = fs.readFileSync(path.join(teamsDir, file), 'utf-8');
        
        // Parse frontmatter
        const frontmatterMatch = content.match(/^---\n([\s\S]*?)\n---/);
        let frontmatter = {};
        let markdownContent = content;
        
        if (frontmatterMatch) {
            const frontmatterText = frontmatterMatch[1];
            markdownContent = content.replace(frontmatterMatch[0], '').trim();
            
            // Simple YAML parser for our needs
            frontmatterText.split('\n').forEach(line => {
                const colonIndex = line.indexOf(':');
                if (colonIndex > 0) {
                    const key = line.substring(0, colonIndex).trim();
                    let value = line.substring(colonIndex + 1).trim();
                    
                    // Remove quotes
                    if (value.startsWith('"') && value.endsWith('"')) {
                        value = value.slice(1, -1);
                    }
                    
                    // Handle nested objects (simple case)
                    if (key.includes('.')) {
                        const [parent, child] = key.split('.');
                        if (!frontmatter[parent]) frontmatter[parent] = {};
                        frontmatter[parent][child] = value;
                    } else {
                        frontmatter[key] = value;
                    }
                }
            });
        }
        
        return {
            params: { slug },
            props: { team: frontmatter, content: markdownContent }
        };
    });
}

const { team, content } = Astro.props;
const { slug } = Astro.params;
---

<MainLayout title={`${team.title} - The Football Family`}>
    <!-- Team Header -->
    <section class="py-16 px-4">
        <div class="container mx-auto">
            <div class="max-w-4xl mx-auto">
                <!-- Team Badge and Basic Info -->
                <div class="flex flex-col md:flex-row items-start md:items-center gap-8 mb-12">
                    <div class="w-32 h-32 rounded-full flex items-center justify-center text-6xl font-bold text-white shadow-2xl"
                         style={`background: linear-gradient(135deg, ${team.colors?.primary || '#333'}, ${team.colors?.secondary || '#666'})`}>
                        {team.title?.charAt(0) || 'T'}
                    </div>
                    
                    <div class="flex-1">
                        <h1 class="text-4xl md:text-6xl font-bold text-white mb-4">{team.title}</h1>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4 text-green-100">
                            <div>
                                <span class="text-green-300">Founded:</span> {team.founded}
                            </div>
                            <div>
                                <span class="text-green-300">Stadium:</span> {team.stadium}
                            </div>
                            <div>
                                <span class="text-green-300">Capacity:</span> {team.capacity?.toLocaleString()}
                            </div>
                            <div>
                                <span class="text-green-300">Manager:</span> {team.manager}
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Quick Stats -->
                <div class="grid grid-cols-2 md:grid-cols-4 gap-6 mb-12">
                    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-4 text-center border border-white/20">
                        <div class="text-2xl font-bold text-white mb-1">{team.statistics?.best_position || 'N/A'}</div>
                        <div class="text-green-200 text-sm">Best Finish</div>
                    </div>
                    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-4 text-center border border-white/20">
                        <div class="text-2xl font-bold text-white mb-1">{team.statistics?.titles || '0'}</div>
                        <div class="text-green-200 text-sm">League Titles</div>
                    </div>
                    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-4 text-center border border-white/20">
                        <div class="text-2xl font-bold text-white mb-1">{team.statistics?.seasons_in_pl || '0'}</div>
                        <div class="text-green-200 text-sm">PL Seasons</div>
                    </div>
                    <div class="bg-white/10 backdrop-blur-sm rounded-lg p-4 text-center border border-white/20">
                        <div class="text-2xl font-bold text-white mb-1">{team.statistics?.average_position || 'N/A'}</div>
                        <div class="text-green-200 text-sm">Avg Position</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Main Content -->
    <section class="px-4 pb-16">
        <div class="container mx-auto">
            <div class="max-w-4xl mx-auto">
                <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
                    <!-- Main Content -->
                    <div class="lg:col-span-2">
                        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-8 border border-white/20">
                            <div class="prose prose-invert max-w-none">
                                <div set:html={content.replace(/\n/g, '<br>')} class="text-green-100 leading-relaxed"></div>
                            </div>
                        </div>
                    </div>

                    <!-- Sidebar -->
                    <div class="space-y-6">
                        <!-- Recent Form -->
                        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20">
                            <h3 class="text-xl font-semibold text-white mb-4">Recent League Positions</h3>
                            <div class="space-y-3">
                                {team.league_positions && Object.entries(team.league_positions).slice(0, 5).map(([season, position]) => (
                                    <div class="flex justify-between items-center">
                                        <span class="text-green-200">{season}</span>
                                        <span class="bg-green-600 text-white px-3 py-1 rounded-full text-sm font-medium">
                                            {position}{position === '1' ? 'st' : position === '2' ? 'nd' : position === '3' ? 'rd' : 'th'}
                                        </span>
                                    </div>
                                ))}
                            </div>
                        </div>

                        <!-- Stadium Info -->
                        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20">
                            <h3 class="text-xl font-semibold text-white mb-4">Stadium Details</h3>
                            <div class="space-y-3">
                                <div>
                                    <span class="text-green-300 block text-sm">Name</span>
                                    <span class="text-white">{team.stadium}</span>
                                </div>
                                <div>
                                    <span class="text-green-300 block text-sm">Capacity</span>
                                    <span class="text-white">{team.capacity?.toLocaleString()}</span>
                                </div>
                                <div>
                                    <span class="text-green-300 block text-sm">Owner</span>
                                    <span class="text-white">{team.owner}</span>
                                </div>
                            </div>
                        </div>

                        <!-- Quick Links -->
                        <div class="bg-white/10 backdrop-blur-sm rounded-xl p-6 border border-white/20">
                            <h3 class="text-xl font-semibold text-white mb-4">Quick Links</h3>
                            <div class="space-y-3">
                                <a href={`/matches?team=${slug}`} class="block text-green-300 hover:text-white transition-colors">
                                    📊 Match Results
                                </a>
                                <a href={`/transfers?team=${slug}`} class="block text-green-300 hover:text-white transition-colors">
                                    💰 Transfer History
                                </a>
                                <a href={`/news?team=${slug}`} class="block text-green-300 hover:text-white transition-colors">
                                    📰 Latest News
                                </a>
                                {team.website && (
                                    <a href={team.website} target="_blank" rel="noopener noreferrer" class="block text-green-300 hover:text-white transition-colors">
                                        🌐 Official Website
                                    </a>
                                )}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Related Teams -->
    <section class="py-16 px-4 bg-black/20">
        <div class="container mx-auto">
            <div class="max-w-4xl mx-auto text-center">
                <h2 class="text-3xl font-bold text-white mb-8">Explore Other Teams</h2>
                <div class="flex flex-wrap justify-center gap-4">
                    <a href="/teams/arsenal" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Arsenal</a>
                    <a href="/teams/manchester-united" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Manchester United</a>
                    <a href="/teams/liverpool" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Liverpool</a>
                    <a href="/teams/manchester-city" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Manchester City</a>
                    <a href="/teams/chelsea" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Chelsea</a>
                    <a href="/teams/tottenham" class="bg-white/10 hover:bg-white/20 px-4 py-2 rounded-lg text-white transition-colors">Tottenham</a>
                </div>
                <div class="mt-8">
                    <a href="/teams" class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-medium transition-colors">
                        View All Teams
                    </a>
                </div>
            </div>
        </div>
    </section>
</MainLayout>